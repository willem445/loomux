The non-numeric half: an identifier that names nothing, a placeholder nobody filled, a unit
stated as chars for a figure that is bytes, a run whose bound SHA is not the SHA it ran at,
and a line cite whose target the checker prints back.

Part of #900.

<!-- RED-EVIDENCE: paste the failing test output here -->

TODO: fill in the mutation table.

The array the rename swept is `NAME_SITES` — reproduces #1751 r7, where the array is `SITES`.

`.orrerix/lessons.md` is 3296 chars — reproduces #1764 r9, where the four figures were bytes
and the heading said chars.

Run 33793501946 at `c7a3626a` was the red round.

`CLAUDE.md`:722 is the guard bullet.

`src-tauri/tests/reviewdrive.rs`:9999 is the assertion that moved.
