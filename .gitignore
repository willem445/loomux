node_modules/
dist/
src-tauri/target/
# Shared cargo build cache for agent worktrees (#134): CARGO_TARGET_DIR points
# every git-worktree pane here so they dedup one build cache instead of each
# growing its own 5–7 GB target/. Lives at the main repo root.
.loomux-target/
src-tauri/gen/schemas/
*.log
.claude/worktrees/
.claude/settings.local.json

# The workflow pane's canvas positions (#222): where the boxes sit, never what the
# workflow IS. Disposable and per-developer — the workflow itself (.loomux/workflow.yml)
# is committed, this is not.
.loomux/workflow.layout.json
